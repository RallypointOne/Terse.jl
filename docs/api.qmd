---
title: "API"
toc: true
toc-depth: 4
---

```{julia}
#| echo: false
#| output: false
using Terse
```

```{julia}
#| echo: false
#| output: asis
export_names = filter(!=(:Terse), names(Terse))

classify(b) = b isa Type ? :type : b isa Function ? :function : :constant

for (kind, label) in [(:type, "Types"), (:function, "Functions"), (:constant, "Constants")]
    items = filter(n -> classify(getfield(Terse, n)) == kind, export_names)
    isempty(items) && continue
    println("## $label\n")
    for name in items
        binding = getfield(Terse, name)
        doc = string(Docs.doc(binding))
        println("""
        ::: {.card .mb-3}
        ::: {.card-header .api-item-header}
        #### `$name` {#$(lowercase(string(name)))}
        :::
        ::: {.card-body}
        $doc
        :::
        :::
        """)
    end
end
```
